<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="App7.Views.AboutPage"
             xmlns:vm="clr-namespace:App7.ViewModels"
             Title="SQL Verbindung">

    <ContentPage.BindingContext>
        <vm:AboutViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="Accent">#96d1ff</Color>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackLayout>

            <Label Text="Bitte geben Sie ihre Datenbank Verbindungsdaten ein" FontSize="Large" Margin="15,20,10,30"/>

            <Entry x:Name="dbadresse" Placeholder="Adresse / Hostname" Margin="10,15,10,10" />

            <Entry x:Name="dbname" Placeholder="Datenbank Name" Margin="10,15,10,10" />

            <Entry x:Name="dbuser" Placeholder="User Name" Margin="10,15,10,10" />

            <Entry x:Name="dbpw" Placeholder="Passwort" Margin="10,15,10,10" IsPassword="True" />

            <Button Text="verbindung testen" Margin="20,50,20,10"/>
            <Button Text="speichern" Margin="20,0,20,10"/>

        </StackLayout>
    </Grid>

</ContentPage>